apiVersion: v1
kind: Service
metadata:
  name: {{ include "personal-site.frontend.serviceName" . }}
  labels:
    {{- include "personal-site.componentLabels" (dict "component" "frontend" "context" $) | nindent 4 }}
  {{- with .Values.frontend.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "personal-site.componentSelectorLabels" (dict "component" "frontend" "context" $) | nindent 4 }}

