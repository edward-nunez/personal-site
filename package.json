{
  "name": "personal-site",
  "version": "1.0.0",
  "description": "This is my personal site source for edwardnunez.io, a blog and portfolio of my latest activity.",
  "author": "Edward Nunez <mail@edwardnunez.io>",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/edward-nunez/personal-site"
  },
  "scripts": {
    "dev:client": "npm run --workspaces client start",
    "dev:server": "npm run --workspaces server start",
    "lint:client": "npm run --workspaces client lint",
    "lint:server": "npm run --workspaces server lint",
    "build": "npm run --workspaces client build",
    "test": "npm run test:client && npm run test:server",
    "test:client": "npm run --workspaces client test",
    "test:server": "echo \"Error: no test specified\" && exit 1",
    "format:client": "npm run --workspaces client format",
    "format:server": "echo \"Error: no format specified\" && exit 1"
  }
}
